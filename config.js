// ============================================================
//  DYNAMIC QR CONFIG — Edit the URLs below to change where
//  each QR code redirects. Save the file and push to GitHub.
// ============================================================

var QR1_URL = "https://example.com";   // ← Change this for QR Code 1
var QR1_LABEL = "QR Code 1";           // ← Friendly name (shown on dashboard)

var QR2_URL = "https://example.com";   // ← Change this for QR Code 2
var QR2_LABEL = "QR Code 2";           // ← Friendly name (shown on dashboard)
